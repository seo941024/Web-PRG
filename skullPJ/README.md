# 해골용사 프로젝트 — 작업 안내

이 폴더에는 게임의 두 버전이 있습니다. **다른 컴퓨터/세션에서 이어서 작업할 때 반드시 아래를 먼저 읽으세요.**

## 📁 skull_V1/ — 개발 완료 (더 이상 수정 안 함)
사이드스크롤 액션 플랫포머 버전. 8직업, 밸런스, 아이템, 스토리까지 완성된 **참고/백업용**입니다.
- 새 기능을 여기 추가하지 마세요.
- 코드 로직(데미지 공식, 아이템 %, 스탯 등)을 v2로 이식할 때 참고 자료로만 사용하세요.

## 📁 skull/ — 현재 개발 중 (여기에 집중)
**탑다운(쿼터뷰) 액션 게임으로 새로 재구축 중.** 앞으로의 모든 작업은 이 폴더에서 진행합니다.

### 진행 상황
- 엔진: 순수 JS + Canvas 2D, `ctx.scale(ZOOM,ZOOM)` 기반 2배 확대 렌더링
- 이동: 8방향 이동, 스프린트(Z, 2배속), 회피(Space, 스태미나 소모 + 잔상)
- 전투: 4타 콤보 시스템(콤보 윈도우 0.4초, 피니시 데미지 보너스), 근접 판정
- 적 AI: 추격 → 예고 → 돌진공격 → 쿨다운 상태머신
- 스프라이트: PixelLab AI로 생성한 도트 캐릭터. `sprites/raw/<직업번호>/` 에 방향별(5방향+자동반전) 원본,
  `anim/<idle|walk|sprint|attack>/<방향>/frame_XX.png` 에 GIF 추출 프레임
- 구현 완료된 직업: **도적(1)만** — 나머지는 스탯/스프라이트 미구현

### 확정된 직업 번호 (core.js 주석 참고)
| 번호 | 직업 | 상태 |
|---|---|---|
| 0 | 성기사(팔라딘) | 미구현 |
| 1 | 도적 | ✅ 완료 (이동/전투/AI) |
| 2 | 마법사 | 미구현 |
| 3 | 버서커 | 미구현 |
| 4 | 발키리 | 미구현 (탄창+재장전 시스템은 빼고 단순화 예정) |
| 5 | 혈귀 | 미구현, **히든 캐릭터**로 취급 |

검사(구 0번)·조커는 로스터에서 삭제 확정.

### 재사용 가능한 구 코드 (`skull/js/`에 그대로 있음, 아직 index.html에 연결 안 됨)
`combat.js`(hitE 데미지 코어), `upgrade_shop.js`(아이템, %기반 밸런스), `story.js`, `audio.js`,
`npc.js`, `systems.js`(스태미나 상수), `render_ui.js` — 필요할 때 하나씩 연결할 것.

### 다음 할 일
1. 성기사·마법사·버서커·발키리(단순화)·혈귀 스프라이트 + `CLASS_PROFILE` 스탯
2. 각 직업 애니(최소 idle/walk) — 프리셋(Walking/Idle/Full Sprint)은 안정적, 커스텀(공격 등)은 크레딧 아끼며 신중히
3. 직업선택 화면, 아이템/상점 연결
4. 맵/스테이지 구조 (지금은 방 1개 하드코딩)

### PixelLab 사용 팁 (겪은 시행착오)
- 캐릭터 생성: Humanoid / v3 / **Low Top-Down** 카메라 / 48px / High Detail
- 방향은 5개만 생성(south, south-east, east, north-east, north) — west 계열은 코드(`sprites.js`)가 좌우반전으로 자동 처리, 크레딧 37.5% 절감
- **애니메이션은 프리셋(Walking/Running/Full Sprint/Idle)이 안정적**, Custom Animation V3는 BETA라 복잡한 동작(회전, 양손 동시)에서 실패 확률이 있음 — 실패해도 유료 결제 후 재시도 여지가 있으니 **과감한 동작도 시도할 것** (보스는 특히 단순 팔동작만으론 부족함, 크고 과격한 모션 필요)
- Pixelorama 편집기에서 "V3 animations... visible frame count of 4,6,8,10,12,14,16" 에러 나면: "Keep first frame" 옵션 때문에 참조프레임이 카운트에서 제외되는 것 — 총 프레임을 목록 값+1로 맞추거나 처음부터 원하는 프레임수로 재생성
- **레이저·투사체 등 원거리 공격은 캐릭터 팔 애니로 정교하게 안 만들어도 됨** — 캐릭터는 짧은 시전 포즈만, 실제 빔/탄환은 `combat.js`의 `spawnLaser`/`spawnEBullet`처럼 코드로 그리는 별도 오브젝트로 처리 (보스도 동일)
